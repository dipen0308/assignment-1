# assignment-1
The code provides a straightforward, file-based system for a teacher or administrator to manage student attendance. It relies on two simple CSV files for persistent data storage and interacts with the user via the command line interface.
